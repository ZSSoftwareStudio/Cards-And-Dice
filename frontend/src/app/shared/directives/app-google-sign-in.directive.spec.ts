import { AppGoogleSignInDirective } from './app-google-sign-in.directive';

describe('AppGoogleSignInDirective', () => {
  it('should create an instance', () => {
    const directive = new AppGoogleSignInDirective();
    expect(directive).toBeTruthy();
  });
});
